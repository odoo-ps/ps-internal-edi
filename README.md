# psbe-internal

Repo to store module usefull for many project

**WARNING : If a module is set as 'installable' = False, it means it is not migrated to the version of the branch. Don't forget to take this into account if doing an estimation**
