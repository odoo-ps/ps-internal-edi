Automatically archive synchronizations :

- add active field on synchronizations
- allow to choose duration before archive synchronizations, per status
- let a cron task archive them depending on the configuration
